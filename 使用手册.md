# Fragrance 引擎使用手册

> **技术栈：** Electron + React 18 + TypeScript + Zustand  
> **最后更新：** 2026-06-03

---

## 目录

1. [快速开始](#1-快速开始)
2. [项目结构](#2-项目结构)
3. [开发命令](#3-开发命令)
4. [剧本编写指南](#4-剧本编写指南)
5. [添加游戏资源](#5-添加游戏资源)
6. [存档系统](#6-存档系统)
7. [键盘快捷键](#7-键盘快捷键)
8. [UI 与交互设计](#8-ui-与交互设计)
9. [设置系统](#9-设置系统)
10. [架构说明](#10-架构说明)
11. [打包与发布](#11-打包与发布)
12. [常见问题](#12-常见问题)

---

## 1. 快速开始

### 环境要求

- Node.js 18+
- npm 9+

### 安装依赖

```bash
cd fragrance
npm install
```

### 快速启动

双击 `dev.bat` 或执行：

```bash
npm run dev
```

Electron 窗口会自动打开（开发端口 `5199`）。Vite 提供 HMR 热更新——修改 React 组件或剧本 JSON 后 UI 立即刷新，无需手动重启。

### 构建生产包

```bash
npm run package
```

在 `release/` 目录下生成 `Fragrance Setup x.x.x.exe` 安装包。

---

## 2. 项目结构

```
fragrance/
│
├── electron/                      # Electron 主进程
│   ├── main/index.ts              # 窗口创建、IPC 存档操作
│   └── preload/index.ts           # contextBridge 安全暴露 API
│
├── src/                           # React 渲染进程（游戏本体）
│   ├── engine/                    # 核心引擎
│   │   ├── types.ts               # 全部指令类型定义
│   │   └── ScriptEngine.ts        # 指令执行、条件求值、跳转
│   ├── stores/
│   │   ├── gameStore.ts           # 游戏运行时状态（phase / bg / char / flags …）
│   │   └── settingsStore.ts       # 用户设置状态（音量 / 速度 / 全屏 …）
│   ├── services/
│   │   ├── SaveManager.ts         # 存档读写（IPC / localStorage）
│   │   └── ResourceManager.ts     # 资源路径解析
│   ├── components/                # UI 组件
│   │   ├── GameScreen.tsx         # 主舞台——按 phase / uiMode 路由界面
│   │   ├── TitleScreen.tsx        # 标题画面（花瓣动画）
│   │   ├── DialogueBox.tsx        # 对话框（浮动卡片样式）
│   │   ├── CharacterDisplay.tsx   # 角色立绘层
│   │   ├── ChoicePanel.tsx        # 分支选项
│   │   ├── NarrationOverlay.tsx   # 旁白
│   │   ├── Background.tsx         # 场景背景
│   │   ├── PauseMenu.tsx          # 暂停菜单（ESC 呼出）
│   │   ├── SettingsPanel.tsx      # 设置面板
│   │   └── SaveLoadScreen.tsx     # 存档/读档界面（10 个槽位）
│   ├── assets/styles/global.css   # 全局样式 & CSS 变量
│   ├── App.tsx                    # 根组件
│   └── main.tsx                   # React 入口
│
├── resources/                     # 游戏资源（打包进 ASAR）
│   └── scripts/
│       └── main_story.json        # 主线剧本
│
├── dev.bat                        # Windows 一键启动开发服务器
├── .gitignore
├── LICENSE                        # MIT 协议
├── electron.vite.config.ts        # electron-vite 构建配置
├── electron-builder.yml           # electron-builder 打包配置
├── package.json
└── 使用手册.md                     # 你正在读的这份文档
```

---

## 3. 开发命令

| 命令 | 用途 |
|------|------|
| `npm run dev` | 启动开发服务器（Electron + Vite HMR，端口 5199） |
| `npm run build` | 编译渲染进程 & 主进程（输出到 `out/`） |
| `npm run preview` | 预览编译后的产物 |
| `npm run package` | 编译 + 打包为 Windows 安装包 |

### 开发流程建议

1. **改剧本为主**：编辑 `resources/scripts/main_story.json`，Vite HMR 自动刷新
2. **改 UI 样式**：编辑 `src/assets/styles/global.css`，立刻看到效果
3. **改引擎逻辑**：编辑 `src/engine/ScriptEngine.ts`，保存后自动重载
4. **测试存档**：开发模式下 Electron 存档在 `userData/saves/`；浏览器直接打开则回退到 `localStorage`

---

## 4. 剧本编写指南

剧本是一个 JSON 数组，每条指令按顺序执行。引擎一次性执行所有非阻塞指令，遇到阻塞指令（`dialogue`/`narration`/`choice`/`end`）时暂停，等待玩家交互。

### 4.1 指令速查表

#### 场景

| 指令 | 说明 | 示例 |
|------|------|------|
| `scene` | 场景标记（jump / branch 的目标） | `{ "type": "scene", "id": "prologue" }` |
| `end` | 结束游戏，返回标题画面 | `{ "type": "end" }` |

#### 视觉

| 指令 | 说明 | 参数 |
|------|------|------|
| `bg` | 切换背景 | `src`: 背景图片, `effect`: "fade"/"dissolve"/"none" |
| `char` | 角色立绘 | `id`/`pose`/`pos`("left"/"center"/"right")/`effect` |
| `cg` | 全屏插画 | `src`: CG 路径, `effect`: 入场特效 |

#### 文本（阻塞指令）

| 指令 | 说明 | 参数 |
|------|------|------|
| `dialogue` | 对话框 | `speaker`: 说话人, `text`: 对话内容 |
| `narration` | 旁白 | `text`: 旁白文字 |

#### 流程控制

| 指令 | 说明 | 参数 |
|------|------|------|
| `choice` | 分支选项（阻塞） | `prompt`: 提示, `options`: 选项数组 |
| `jump` | 无条件跳转 | `target`: 目标 scene.id |
| `branch` | 条件分支 | `cond`: 条件表达式, `then`/`else`: 跳转目标 |
| `setFlag` | 设置变量 | `key`: 变量名, `value`: 值 |
| `wait` | 等待 | `ms`: 毫秒数 |

#### 音频

| 指令 | 说明 | 参数 |
|------|------|------|
| `bgm` | 播放背景音乐 | `src`/`loop`/`volume`(0-1) |
| `sfx` | 播放音效 | `src`: 音效文件 |

### 4.2 选项 (choice) 详解

```json
{
  "type": "choice",
  "prompt": "接下来要怎么做？",
  "options": [
    {
      "text": "和小羽一起去社团教室",
      "next": { "type": "jump", "target": "club_room" },
      "setFlag": { "affection": 2 }
    },
    {
      "text": "先去图书馆查点资料",
      "next": { "type": "jump", "target": "library" },
      "setFlag": { "affection": 0 }
    },
    {
      "text": "（找个借口回教室）",
      "next": { "type": "jump", "target": "back_to_classroom" },
      "setFlag": { "affection": -1 }
    },
    {
      "text": "（二周目专属选项）",
      "next": { "type": "jump", "target": "hidden_route" },
      "cond": "flags.week2"
    }
  ]
}
```

- **`text`**：显示给玩家的选项文字
- **`next`**：选中后跳转到哪个场景（必须是 `jump` 指令对象）
- **`setFlag`**：选中后自动修改哪些 flag（可选）
- **`cond`**：显示条件（可选），用于条件解锁（如二周目专属选项）

### 4.3 条件表达式语法

| 表达式 | 含义 |
|--------|------|
| `flags.affection >= 2` | affection ≥ 2 |
| `flags.met_xiaoyu_morning` | met_xiaoyu_morning 为 truthy |
| `!flags.already_slept` | already_slept 为 falsy |
| `flags.day >= 3 && flags.affection > 1` | 组合条件 |
| `flags.week2 && flags.affection >= 12` | 二周目 + 高好感度 |

> 条件基于 `new Function()` 动态求值，支持 JavaScript 比较运算符。

### 4.4 编写建议

1. **总是以 `scene` 开头**——跳转锚点
2. **对话前先设置好 bg 和 char**
3. **用描述性的 scene id**——如 `classroom_first_meeting` 而非 `scene01`
4. **一个剧本文件放一个章节**
5. **先画分支流程图再写剧本**

---

## 5. 添加游戏资源

### 5.1 添加角色

在 `Assets/Resources/Characters/` 下以角色 ID 建目录，表情图命名：`{角色ID}_{表情名}.png`

```
Assets/Resources/Characters/heroine/Expressions/
    ├── heroine_normal.png
    ├── heroine_happy.png
    └── heroine_sad.png
```

剧本引用：
```json
{ "type": "char", "id": "heroine", "pose": "happy", "pos": "center" }
```

### 5.2 添加背景

```json
{ "type": "bg", "src": "classroom_day", "effect": "fade" }
```

### 5.3 添加 BGM / 音效

```json
{ "type": "bgm", "src": "daily_theme", "loop": true, "volume": 0.7 }
{ "type": "sfx", "src": "door_open" }
```

> ⚠️ **当前音效功能日志输出已就绪，实际音频播放逻辑待开发。**

---

## 6. 存档系统

### 6.1 运行机制

```
渲染进程 (React)          主进程 (Electron)
     │                         │
     ├─ "save:write" ────▶  fs.writeFile
     ├─ "save:read"  ────▶  fs.readFile
     └─ "save:list"  ────▶  fs.readdir
```

- **存储位置**：`%APPDATA%/fragrance/saves/`
- **文件命名**：`slot_0.json` ~ `slot_9.json`（10 个槽位）
- **浏览器回退**：非 Electron 环境自动使用 `localStorage`（key 前缀 `fragrance_save_`）

### 6.2 存档结构

```json
{
  "slot": 0,
  "savedAt": "2026-06-03T14:30:00.000Z",
  "label": "小羽：早上好……",
  "cursorIndex": 12,
  "flags": { "affection": 2, "day": 1 }
}
```

| 字段 | 说明 |
|------|------|
| `slot` | 槽位编号 (0-9) |
| `savedAt` | 存档时间 (ISO 8601) |
| `label` | 存档标签（当前对话前 30 字） |
| `cursorIndex` | 剧本游标位置 |
| `flags` | 全部游戏变量快照 |

### 6.3 二周目系统

通关后（从游戏返回标题画面），`localStorage` 中存入 `fragrance_completed = "1"`。下次开始新游戏时，引擎自动注入 `flags.week2 = true`，解锁选项中的 `cond: "flags.week2"` 条件，实现隐藏路线。

### 6.4 如何扩展存档

编辑 `src/engine/types.ts` 的 `SaveData` 接口，在 `SaveManager.write()` 中写入额外字段，在 `GameScreen.handleLoad()` 中恢复。

---

## 7. 键盘快捷键

| 界面 | 按键 | 功能 |
|------|------|------|
| 标题画面 | `Enter` | 开始新游戏 |
| 标题画面 | `Escape` | 退出应用 |
| 游戏中 | `Escape` | 打开暂停菜单 |
| 暂停菜单 | `Escape` | 返回游戏 |
| 存档/读档 | `Escape` | 返回游戏 |
| 设置面板 | `Escape` | 返回游戏 |

点击 / 鼠标交互覆盖其余操作：点击对话框推进剧情、点击选项选择分支。

---

## 8. UI 与交互设计

### 8.1 标题画面

- 暖色调文艺风格，28 片花瓣飘落动画
- 三处光斑呼吸效果
- 自定义钢笔笔尖光标（hover/click 状态变化）
- 五个菜单按钮（开始 / 继续 / 相册 / 设置 / 退出），带渐入动画
- "继续"按钮在无存档时呈灰色禁用状态

### 8.2 暂停菜单（游戏内 ESC）

- 暖色半透明毛玻璃遮罩
- 五个按钮：返回游戏 / 保存进度 / 读取存档 / 设置 / 返回主页
- ESC 关闭，逐层返回

### 8.3 对话框

- 浮动卡片设计：暖色渐变背景、顶部金线装饰条、角落菱形点缀
- 说话人标签：方框式容器 + 左侧强调线
- 圆形脉冲继续指示器

### 8.4 选项面板

- 暖色半透明遮罩
- 左侧强调线 hover 滑动效果

### 8.5 全屏模式

设置面板中可切换全屏。全屏模式下 ESC 不会退出全屏——改为呼出暂停菜单。通过 F11 或窗口控件退出全屏时，设置面板开关自动同步。

---

## 9. 设置系统

设置面板（从标题画面或暂停菜单进入）包含：

| 选项 | 控制方式 | 默认值 | 说明 |
|------|----------|--------|------|
| 文字速度 | ◀ ▶ 步进 | 40 (标准) | 5~100 ms/字 |
| 自动播放 | 开关 + 间隔 | 关闭 / 3s | 开启后按间隔自动推进剧情 |
| 背景音乐 | ◀ ▶ + 进度条 | 70% | 0~100%，步进 10% |
| 音效音量 | ◀ ▶ + 进度条 | 80% | 同上 |
| 全屏模式 | 开关 | 关闭 | 浏览器全屏 API |

所有设置通过 `localStorage`（key: `fragrance_settings`）持久化，带版本号检测，结构升级时自动重置。

---

## 10. 架构说明

### 10.1 数据流

```
剧本 JSON ──▶ ScriptEngine ──▶ Zustand Store ◀── React 组件
                                     │
                              SaveManager (IPC)
                                     │
                              本地文件系统
```

1. `ScriptEngine` → 逐条解析指令 → 更新 `gameStore`
2. React 组件订阅 store → 自动渲染
3. 用户交互 → 回调 `ScriptEngine.continue()` 或 `handleChoice()`
4. 存档 → `SaveManager` 通过 IPC 写入本地

### 10.2 阻塞模型

引擎的 `advance()` 对非阻塞指令一口气执行到底，遇阻塞指令（`dialogue`/`narration`/`choice`/`end`）暂停。因此：
- 批量设置（多个 flag、换背景+换 BGM）会被一次性处理
- 对话和旁白需要玩家点击才能继续

### 10.3 Zustand Store 结构

**gameStore** — 游戏运行时状态：

```typescript
{
  phase: 'title' | 'playing' | 'narration' | 'choice' | 'save' | 'load' | 'cg',
  background, characters[], dialogue, narration,
  choices[], choicePrompt, bgm,
  flags: Record<string, unknown>,
  cursorIndex: number
}
```

**settingsStore** — 用户设置（持久化）：

```typescript
{
  textSpeed, autoPlay, autoPlayDelay,
  bgmVolume, sfxVolume, fullscreen
}
```

---

## 11. 打包与发布

```bash
npm run package
```

- 输出：`release/Fragrance Setup x.x.x.exe`
- 所有代码和资源打包进 `app.asar`，玩家无法直接浏览
- 存档路径：`%APPDATA%/fragrance/saves/`（ASAR 外，保证可读写）

---

## 12. 常见问题

### Q: 改了剧本但没有生效？

A: 确认 JSON 语法正确，检查 DevTools Console 是否有报错。

### Q: 角色立绘不显示？

A: 确认图片在 `Assets/Resources/Characters/{角色ID}/Expressions/` 下，文件名格式正确。

### Q: 如何调试剧本执行？

A: DevTools 会在开发模式下自动打开（`openDevTools({ mode: 'detach' })`）。可在 `ScriptEngine.ts` 的 `advance()` 中添加 `console.log(cmd)` 追踪指令。

### Q: 存档后读档位置不对？

A: 剧本修改导致指令行号变化。发布前锁定剧本版本。

### Q: 如何实现章节切换？

A: 把所有章节写在同一 JSON 中（用 `scene` + `jump` 分隔），或修改 `ScriptEngine.load()` 支持多文件。

### Q: 设置无法保存？

A: 检查 `localStorage` 是否有异常。设置使用 `fragrance_settings` key，带版本号 `_version` 字段。若版本不匹配会自动重置为默认值。

### Q: 实现多结局 / 隐藏路线？

A: 使用 `flags` + `branch` 在关键节点分流。二周目隐藏路线可利用 `fragrance_completed` 标记 + `cond: "flags.week2"` 选项条件。

---

## 附录：技术对比

| 对比项 | Fragrance 引擎 | Ren'Py | TyranoBuilder | Unity + Fungus |
|--------|---------------|--------|---------------|----------------|
| 上手门槛 | 低（Web 技术栈） | 低（Python DSL） | 极低（可视化） | 高（C# + Editor） |
| 定制自由度 | 极高（完整前端能力） | 高 | 中 | 极高 |
| 包体大小 | ~80MB | ~50MB | ~60MB | ~150MB+ |
| 热更新 | 天然支持 | 需额外方案 | 不支持 | 需 XLua 等 |
| 跨平台 | Win/Mac/Linux | Win/Mac/Linux/Android | Win/Mac | 全平台 |
| 开源协议 | MIT | MIT | 闭源 | 混合 |

---

如有问题，请参考项目源码中的注释，或在 `src/engine/` 下查看核心逻辑。
